<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Your Orders | MyStore</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: #f5f6fa;
    }
    h2 {
      font-weight: 700;
      color: #1e3a8a;
      margin-bottom: 30px;
      text-align: center;
    }
    .order-card {
      border-radius: 15px;
      background: #ffffff;
      padding: 25px;
      margin-bottom: 25px;
      box-shadow: 0 5px 20px rgba(0,0,0,0.08);
      transition: transform 0.3s, box-shadow 0.3s;
    }
    .order-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 30px rgba(0,0,0,0.15);
    }
    .order-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 15px;
    }
    .status-timeline {
      display: flex;
      justify-content: space-between;
      margin-bottom: 20px;
      position: relative;
    }
    .status-step {
      flex: 1;
      text-align: center;
      font-size: 0.85rem;
      color: #6c757d;
      position: relative;
      font-weight: 500;
    }
    .status-step.active {
      color: #0d6efd;
      font-weight: 600;
    }
    .status-step::before {
      content: '';
      width: 14px;
      height: 14px;
      border-radius: 50%;
      background: #dee2e6;
      display: block;
      margin: 0 auto 8px;
      z-index: 1;
      position: relative;
    }
    .status-step.active::before {
      background: #0d6efd;
    }
    .status-step:not(:last-child)::after {
      content: '';
      position: absolute;
      top: 6px;
      left: 50%;
      width: 100%;
      height: 3px;
      background: #dee2e6;
      z-index: 0;
      transform: translateX(8px);
    }
    .status-step.active:not(:last-child)::after {
      background: #0d6efd;
    }
    .product-scroll {
      display: flex;
      overflow-x: auto;
      gap: 15px;
      padding-bottom: 10px;
      margin-bottom: 20px;
    }
    .product-scroll::-webkit-scrollbar {
      height: 6px;
    }
    .product-scroll::-webkit-scrollbar-thumb {
      background: #0d6efd;
      border-radius: 3px;
    }
    .product-item {
      flex: 0 0 auto;
      width: 90px;
      cursor: pointer;
      transition: transform 0.2s;
      text-align: center;
    }
    .product-item:hover {
      transform: translateY(-5px);
    }
    .product-item img {
      width: 90px;
      height: 90px;
      object-fit: cover;
      border-radius: 8px;
      margin-bottom: 5px;
    }
    .product-item span {
      font-size: 0.8rem;
      display: block;
      overflow: hidden;
      white-space: nowrap;
      text-overflow: ellipsis;
    }
    .total-section {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .btn-view-details {
      border-radius: 30px;
      padding: 8px 20px;
    }
    .empty-state {
      text-align: center;
      padding: 80px 20px;
      color: #6c757d;
    }
    .empty-state i {
      font-size: 120px;
      color: #d1d5db;
    }
    @media (max-width: 768px) {
      .product-item {
        width: 70px;
      }
      .product-item img {
        width: 70px;
        height: 70px;
      }
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <h2>🧾 Your Orders</h2>

    <% if (orders && orders.length > 0) { %>
      <% orders.forEach(order => { %>
        <div class="order-card">
          <!-- Header -->
          <div class="order-header">
            <h6 class="text-truncate" title="Order ID: <%= order._id %>">
              Order: <span class="text-secondary"><%= order._id %></span>
            </h6>
            <span class="badge 
              <%= order.status === 'Pending' ? 'bg-warning text-dark' : order.status === 'Completed' ? 'bg-success' : 'bg-secondary' %>">
              <%= order.status %>
            </span>
          </div>

          <!-- Status Timeline -->
          <div class="status-timeline">
            <div class="status-step <%= order.status === 'Pending' ? 'active' : '' %>">Pending</div>
            <div class="status-step <%= order.status === 'Shipped' || order.status === 'Completed' ? 'active' : '' %>">Shipped</div>
            <div class="status-step <%= order.status === 'Completed' ? 'active' : '' %>">Delivered</div>
          </div>

          <!-- Products Scroll -->
          <div class="product-scroll">
            <% order.products.forEach(p => { %>
              <% if (p.productId && p.productId.name) { %>
                <div class="product-item" onclick="window.location='/product/<%= p.productId._id %>'">
                  <img src="<%= p.productId.image || '/images/default-product.png' %>" alt="<%= p.productId.name %>">
                  <span><%= p.productId.name %></span>
                </div>
              <% } else { %>
                <div class="product-item">
                  <img src="/images/default-product.png" alt="Unavailable">
                  <span>Unavailable</span>
                </div>
              <% } %>
            <% }) %>
          </div>

          <!-- Total & View -->
          <div class="total-section">
            <h6 class="mb-0">Total: <span class="text-success">$<%= order.totalPrice.toFixed(2) %></span></h6>
            <a href="/orders/<%= order._id %>" class="btn btn-primary btn-sm btn-view-details shadow-sm">View Details</a>
          </div>
        </div>
      <% }) %>
    <% } else { %>
      <div class="empty-state">
        <i class="bi bi-bag-check"></i>
        <h4 class="mt-3">No orders yet</h4>
        <p>Start shopping and your orders will appear here.</p>
        <a href="/" class="btn btn-outline-primary mt-3">← Back to Store</a>
      </div>
    <% } %>
  </div>
</body>
</html>
