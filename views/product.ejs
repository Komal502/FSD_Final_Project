<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= product.name %> | E-Shop</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    .product-img {
      border-radius: 12px;
      object-fit: cover;
      width: 100%;
      height: 400px;
    }
    .btn-add-cart {
      border-radius: 50px;
      font-weight: 600;
      padding: 10px 25px;
    }
    .price {
      font-size: 1.8rem;
      font-weight: 700;
      color: #43a047;
    }
    h2 {
      font-weight: 700;
      color: #0d6efd;
    }
    .quantity-input {
      width: 80px;
      border-radius: 8px;
    }
  </style>
</head>
<body>
<div class="container mt-5">
  <a href="/" class="btn btn-outline-secondary mb-3"><i class="bi bi-arrow-left"></i> Back to Store</a>
  <div class="row g-4">
    <div class="col-md-6">
      <img src="<%= product.image %>" class="product-img shadow-sm" alt="<%= product.name %>">
    </div>
    <div class="col-md-6">
      <h2><%= product.name %></h2>
      <p class="text-muted"><%= product.description %></p>
      <h3 class="price">₹<%= product.price.toFixed(2) %></h3>

      <form action="/cart/add" method="POST" class="mt-4 d-flex align-items-center gap-3">
        <input type="hidden" name="productId" value="<%= product._id %>">
        <input type="number" name="quantity" value="1" min="1" class="form-control quantity-input">
        <button type="submit" class="btn btn-success btn-add-cart"><i class="bi bi-cart-plus"></i> Add to Cart</button>
      </form>
    </div>
  </div>
</div>
</body>
</html>
